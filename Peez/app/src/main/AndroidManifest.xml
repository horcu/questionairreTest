<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.horcu.apps.peez" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.USE_CREDENTIALS" />
    <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <permission
        android:name="com.example.gcm.permission.C2D_MESSAGE"
        android:protectionLevel="signature" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:logo="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.Translucent" >
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <receiver
            android:name="com.google.android.gms.gcm.GcmReceiver"
            android:permission="com.google.android.c2dm.permission.SEND" >
            <intent-filter>
                <action android:name="com.google.android.c2dm.intent.RECEIVE" />
                <!-- for Gingerbread GSF backward compat -->
                <action android:name="com.google.android.c2dm.intent.REGISTRATION" />

                <category android:name="com.horcu.apps.peez" />
            </intent-filter>
        </receiver>

        <service
            android:name=".service.GcmService"
            android:exported="false" >
            <intent-filter>
                <action android:name="com.google.android.c2dm.intent.RECEIVE" />
            </intent-filter>
        </service>
        <service
            android:name=".service.LoggingService"
            android:exported="false" />
        <service
            android:name=".service.TaskSchedulerService"
            android:exported="true"
            android:permission="com.google.android.gms.permission.BIND_NETWORK_TASK_SERVICE" >
            <intent-filter>
                <action android:name="com.google.android.gms.gcm.ACTION_TASK_READY" />

                <category android:name="com.horcu.apps.peez" />
            </intent-filter>
        </service>

        <activity
            android:name=".ui.activities.MainActivity"
            android:logo="@mipmap/ic_launcher" />
        <activity
            android:name=".ui.activities.TokenActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".ui.activities.GroupActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".ui.addressbook.SelectActivity"
            android:windowSoftInputMode="adjustPan" >
            <intent-filter>
                <action android:name="android.intent.action.PICK" />

                <category android:name="com.google.android.gcm.demo.intent.category.SENDER_ID" />
                <category android:name="com.google.android.gcm.demo.intent.category.API_KEY" />
                <category android:name="com.google.android.gcm.demo.intent.category.TOKEN" />
                <category android:name="com.google.android.gcm.demo.intent.category.TOPIC" />
                <category android:name="com.google.android.gcm.demo.intent.category.GROUP" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activities.GCMActivity"
            android:label="@string/title_activity_prelim" >
        </activity>
        <activity
            android:name=".ui.activities.InviteActivity"
            android:label="@string/title_activity_invite" />
        <activity
            android:name=".ui.activities.PrelimActivity"
            android:label="@string/title_activity_prelim" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activities.BetActivity"
            android:label="@string/title_activity_bet"
            android:parentActivityName=".ui.activities.PrelimActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ui.activities.PrelimActivity" />
        </activity>
        <activity
            android:name=".ui.activities.GameActivity"
            android:label="@string/title_activity_game" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ui.activities.MainActivity" />
        </activity>
        <activity
            android:name=".ui.activities.DeepLinkActivity"
            android:label="@string/invitation_dl" />

        <!-- Required receiver to get broadcasts from the Play Store after application install -->
        <!-- [START referral_receiver_xml] -->
        <receiver
            android:name=".ui.activities.ReferrerReceiver"
            android:exported="true"
            tools:ignore="ExportedReceiver" >
            <intent-filter>
                <action android:name="com.android.vending.INSTALL_REFERRER" />
            </intent-filter>
        </receiver>
        <!-- [END referral_receiver_xml] -->
        <activity
            android:name=".ui.activities.TestBetActivity"
            android:label="@string/test"
            android:parentActivityName=".ui.activities.MainActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.horcu.apps.peez.ui.activities.MainActivity" />
        </activity>
    </application>

</manifest>
